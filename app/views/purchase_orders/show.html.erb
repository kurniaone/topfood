<%= render('shared/orders-tab.html.erb') if current_user.su? %>

<div class="content">
  <h2>Purchase Order</h2>

  <div class="inner">
    <%= render '/orders/show', order_class: PurchaseOrder, order: @order %>
  </div>
</div>
